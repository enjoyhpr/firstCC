# 前端面试题

## 一、TypeScript 在 Vue3 中的作用与具体使用场景

**问题：** Ts在vue3中有什么用，你具体用在哪些方面？

**注意点：** 聚焦类型安全落地价值，结合组件、API、状态管理等实际场景说明，突出与Vue3的适配性

### 1. 作用归纳

- (1) 提供静态类型检查，及早发现类型不匹配与缺失字段，降低运行时错误风险。
- (2) 提升编辑器智能感知与重构能力，减少重构后引入的错误。

### 2. 具体使用场景

- (1) **API 层**：为请求参数与响应体定义类型，明确后端契约并支持自动化类型生成或校验。
- (2) **组件层**：使用类型约束组件输入/输出（如 props、emits），明确组件接收参数的类型、必填性，确保组件间数据传递规范，减少使用错误。
- (3) **Composable 与工具函数**：通过泛型或具体类型定义复用逻辑的输入输出，适配不同业务场景的同时，保证复用过程中的类型一致性。
- (4) **状态管理**：为全局/模块化状态、action、getter 提供类型，保证调用端的正确性。

### 3. 设计层面建议

- (1) 将公共类型集中管理（如 types 或 interfaces 目录），按领域拆分以便维护。
- (2) 把运行时校验与静态类型区分开来：必要时采用运行时校验库（如 zod、ajv）补足类型在运行时的保障。

> **简略总结**：TypeScript保障接口契约与类型安全，提升开发效率与可维护性，建议将类型治理纳入CI流程并配合运行时校验。

---

## 二、网络请求封装的分层与设计思路

**问题：** 有没有对网络请求做过一次封装，请详细说下具体怎么封装，封装了几层？

**注意点：** 明确各层职责边界，重点说明拦截器的实际作用，结合请求防重、取消等实用功能展开

### 1. 常见分层

- (1) **HTTP 客户端实例层**：创建单例客户端（常用库：axios 或 fetch 封装），负责基础配置、超时、证书与跨域策略。
- (2) **通用请求封装层（request helper）**：统一参数与响应格式、业务码解析、重试与超时策略、上传/下载处理、错误统一包装。
- (3) **模块化 API 层**：按业务域拆分的接口集合，仅负责参数映射与类型声明。
- (4) **业务/服务层（可选）**：对多个接口进行组合或封装高阶业务逻辑，如分页器、批量合并或幂等封装。

### 2. 拦截器与中间件职责

- (1) **请求拦截**：统一注入token等鉴权信息、设置公共请求头，通过请求参数生成唯一标识实现防重，同时可关联全局loading状态控制。
- (2) **响应拦截**：统一解析业务 code、处理鉴权失效（refresh token 或跳转登录）、做统一错误上报与日志埋点。

### 3. 设计层面建议

- (1) 定义统一的 `ApiResult<T>` 类型和错误模型，便于上层统一处理与监控。
- (2) 在请求层暴露取消能力（AbortController）与可配置的重试策略，避免业务层手动处理细节。
- (3) 将网络逻辑与 UI 解耦，组件只与业务 API 层交互，不直接依赖底层实现。

> **简略总结**：网络层采用三至四层架构，拦截器处理通用职责，暴露统一类型与取消/重试能力，兼顾可观测性与可替换性。

---

## 三、网络请求中的错误处理责任与策略

**问题：** 在网络请求中是什么对错误处理做出处理的？

**注意点：** 按分层明确错误处理职责，区分HTTP错误与业务错误，突出重试策略和全局收敛的实际意义

### 1. 责任划分

- (1) **底层拦截器**：负责把 HTTP/网络错误转成统一错误对象，处理低级别重试或超时，并触发鉴权恢复流程。
- (2) **request helper**：负责解析后端业务码并按需抛出业务异常或返回降级数据。
- (3) **组件/页面**：对业务相关错误做局部化处理与用户提示，决定是否重试或回退。
- (4) **全局错误收敛**：未捕获异常在全局错误处理器处收敛并上报监控与埋点。

### 2. 设计层面建议

- (1) 统一错误类型与错误码映射表，便于自动化处理与告警分级。
- (2) 在关键路径上增加埋点并保留原始请求上下文，便于事后诊断。
- (3) 对于可重试的错误（短暂网络抖动、限流）使用**指数退避策略**，即重试间隔逐渐延长，避免频繁请求影响服务；幂等性操作需明确重试规则，或由后端保障幂等性。

> **简略总结**：错误处理按层分责，底层管转换与鉴权恢复，业务层管语义化处理，系统需具备可观测与分级告警能力。

---

## 四、组件封装经验与最复杂组件的设计思路

**问题：** 有没有封装过组件，请举例说明个自己认为最复杂的组件？

**注意点：** 以职责分离为核心，用文件上传、虚拟列表、大图片预览组件举例时，清晰拆分架构层次，说明各层解决的核心问题

### 1. 常见封装类型

- (1) 基础 UI 组件（按钮、输入、弹窗）与表单控件。
- (2) 功能组件（上传、播放器、裁剪、富文本、选择器）。

### 2. 典型复杂组件：文件上传（设计要点）

**功能需求：** 多文件选择、前端压缩/裁剪、分片上传、断点续传、并发控制、进度与状态管理、失败重试、预览与签名直传支持。

**技术挑战：** 浏览器兼容、内存与 CPU 限制、分片校验（hash）、网络中断恢复、并发控制与队列策略。

**架构建议：** 将上传抽象为三层——
- 上传服务层：负责底层传输、分片逻辑与服务端交互
- composable层：管理上传状态、任务队列与重试策略
- 展现组件：仅处理用户交互与状态展示，实现逻辑与UI解耦

**测试与可靠性：** 对关键状态机进行单元测试，使用集成/端到端测试验证断网/续传/合并等场景。

### 3. 典型复杂组件：Table 虚拟列表（设计要点）

**功能需求：** 支持大数据量（万级及以上）表格渲染、列固定/拖动/排序、行选择/编辑、数据筛选/搜索、滚动流畅无卡顿。

**技术挑战：** 减少DOM节点数量避免页面卡顿、计算可视区域数据与偏移量、处理固定列与滚动列的同步对齐、兼容复杂单元格内容（如嵌套组件、合并单元格）。

**架构建议：** 采用"核心计算层+渲染层+交互层"拆分——
- 核心计算层：负责可视区域数据截取、滚动偏移量计算、列宽行高缓存
- 渲染层：仅渲染可视区域内单元格，通过绝对定位实现虚拟滚动效果
- 交互层：封装列操作、行操作等通用交互逻辑，与渲染层解耦

**性能优化：**
- 使用 `requestAnimationFrame` 处理滚动计算避免布局抖动
- 缓存已渲染单元格DOM减少重绘重排
- 通过节流处理列拖动与排序事件
- 大数据筛选搜索建议结合后端分页或前端 WebWorker 处理

### 4. 典型复杂组件：大图片预览（设计要点）

**功能需求：** 支持超大图片（如几十MB高清图）加载与缩放、拖拽平移、旋转/翻转、缩略图导航、多图切换、手势操作（移动端）。

**技术挑战：** 解决大图片加载卡顿与内存溢出问题、实现流畅的缩放平移交互、处理图片加载过程中的占位与容错、保证多设备交互体验一致性。

**架构建议：** 采用"加载服务层+核心控制层+视图层"拆分——
- 加载服务层：负责大图片分片加载（如基于Blob切片）、渐进式加载（先低清后高清）、图片缓存策略
- 核心控制层：管理图片状态（缩放比例、位置、旋转角度）、处理手势与鼠标事件转换、计算图片渲染边界
- 视图层：负责图片渲染、遮罩层、控制按钮等UI展示，通过CSS transform实现无卡顿变换

**可靠性保障：**
- 对图片加载失败提供重试与降级显示
- 限制最大缩放比例避免内存压力
- 监听页面卸载时释放图片资源
- 移动端适配不同屏幕分辨率与手势事件（如双指缩放、单指拖动）

> **简略总结**：复杂组件强调职责分离，业务逻辑放可复用模块，UI专注渲染交互，同时保障可测试性与可替换性。

---

## 五、H5 页面调用摄像头等移动端原生 API 的设计思路

**问题：** 如何在h5网页中调用前置摄像头等移动端原生api？

**注意点：** 强调能力分级与降级策略，关注权限请求的用户体验，结合兼容性和性能优化说明

### 1. 能力分级与优先策略

- (1) **优先使用浏览器原生能力**（MediaDevices API）获取视频流以支持实时交互。
- (2) **降级方案**：若仅需拍照或兼容性要求高，使用 `<input type="file" accept="image/*" capture>` 作为降级方案。
- (3) **混合 App 场景**：通过 JSBridge 或插件调用原生相机以获取更稳定的权限與高级控制。

### 2. 设计层面注意事项

- (1) **权限与 UX**：权限请求应在用户交互触发并给出清晰的降级提示与重试路径。
- (2) **兼容性**：考虑 iOS Safari 对某些约束（如 facingMode）的兼容差异，必要时使用设备枚举并按 deviceId 选择。
- (3) **性能**：在需要处理视频帧的场景，考虑在 WebWorker 或使用 requestAnimationFrame 降低主线程负担，并控制帧率与分辨率以降低 CPU 与内存占用。

> **简略总结**：按优先级选择摄像头能力，保障权限UX与兼容性，必要时通过原生能力补足浏览器局限。

---

## 六、微信小程序中除本地缓存外的其他存储方法与设计考量

**问题：** 在微信小程序中除了存在本地缓存中，还有什么存储数据的方法？

**注意点：** 明确各存储方式的适用场景与容量限制，重点说明敏感数据的安全处理方式

### 1. 可选的存储方式

- (1) **文件系统（File API）**：用于保存大文件或导出数据，支持持久化管理，适合图片、视频等资源，需注意区分临时文件与持久文件的使用差异。
- (2) **云开发（云数据库 / 云存储）**：无需自建后端，支持跨设备同步，提供权限管控，云存储单个文件最大50MB，适合中小型项目的非敏感数据存储。
- (3) **后端持久化**：通过 API 将数据保存到后端数据库，适用于业务数据与统一权限控制。
- (4) **应用内内存（App().globalData）**：用于短期跨页面共享轻量数据，非持久化，小程序重启后丢失，避免存储大量数据导致内存溢出。

### 2. 设计层面建议

- (1) 根据数据特性选择存储方式：小量配置用本地缓存（单个key最大1MB，总容量10MB），大文件用文件系统，跨设备数据用云存储或后端，实现资源合理分配。
- (2) 对敏感数据做本地加密或只保留最小必要信息，避免隐私泄露。

> **简略总结**：依数据特性选存储方式，兼顾安全、同步与离线策略，明确各方式容量限制。

---

## 七、在页面中发起多请求但途中切换页面时取消剩余请求的策略

**问题：** 在微信小程序中有个情况是怎么在A页面调用了10个接口，但在进行到第5个接口时切换页面了，怎么阻止剩余5个接口的调用？

**注意点：** 核心是请求与页面生命周期绑定，说明全局请求管理的实现逻辑，区分普通请求与关键请求的处理差异

### 1. 核心策略

- (1) 把每个请求与页面/组件生命周期绑定，发起请求时注册可取消控制器（AbortController 或请求库提供的 cancel token）。
- (2) 在页面卸载或路由切换时统一取消未完成的请求，避免无意义的网络与资源占用。

### 2. 设计层面实现建议

- (1) 在请求封装层实现全局 **RequestManager**，按页面路径作为key管理请求控制器集合，监听路由变化或组件卸载事件，批量取消对应页面的未完成请求，释放资源。
- (2) 对于串行流程，在每一步开始前检查取消标志，避免继续发起后续调用。
- (3) 对于必须完成的关键请求，采用后台任务或后端异步化处理并通过回调/消息通知前端结果，降低前端强一致性负担。

> **简略总结**：请求生命周期绑定页面生命周期，封装层提供统一管理能力，关键请求采用后端异步化或幂等保障。
